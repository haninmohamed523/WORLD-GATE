# Set these only in Vercel's Environment Variables. Do not commit a real .env file.
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxx
# Must be a verified Resend sender, e.g. requests@your-verified-domain.sa
RESEND_FROM_EMAIL=requests@your-verified-domain.sa
